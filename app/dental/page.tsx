import { DentalApp } from "@/components/dental/dental-app";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Gosol Dental — نظام إدارة العيادة",
};

export default function DentalPage() {
  return <DentalApp />;
}
